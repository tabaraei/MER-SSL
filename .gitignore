# --- Python ---
__pycache__/
*.pyc
*.pyo
*.ipynb_checkpoints

# --- Environments ---
.venv/
venv/
.env

# --- Model weights & large tensors / embeddings ---
*.pt
*.pth
*.ckpt
*.npy

# --- Dataset (kept out of the repo; see data/README.md) ---
data/pmemo/
data/*.pkl

# --- Results (checkpoints, plots) — keep the folder, ignore contents ---
results/
!results/.gitkeep

# --- Experiment tracking ---
wandb/
mlruns/

# --- Logs / OS ---
*.log
.DS_Store

# --- Excluded from the submission repo (kept on disk on the server) ---
# Active thesis sources, working reports/journals, Claude-Code meta, and the
# leftover MERT/ tree (upstream license + venv + on-disk embeddings/checkpoints).
overleaf_draft/
reports/
MERT/
JOURNEY.md
blueprint.md
CLAUDE.md
.claude/
.mcp.json
